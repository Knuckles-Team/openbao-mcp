# OpenBao MCP

OpenBao Secrets Engine MCP Server for Agentic AI!

## Environment Variables
- `OPENBAO_URL`: The base service URL.
- `OPENBAO_TOKEN`: The API / bearer token for authentication.

## CLI Usage
```bash
python -m openbao_mcp --help
```
