# Provider workflow catalog

Load only the workflow relevant to the current request.

- [secret-vault-manager](../../secret-vault-manager/WORKFLOW.md): Secret Vault Manager atomic skill. Performs unsealing, initialization, secrets engine mounting, and KV secrets write/read operations on OpenBao (Vault) using openbao-mcp.
